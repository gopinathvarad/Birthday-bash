# Relationship photos

Only optimized, metadata-stripped WebP photographs are deployed from this folder. The original JPEG files are preserved in the private `Ritika-private-originals/photos` backup outside the project.

Update the paths in `src/memoryData.js` if a photograph is replaced. The story dialog supports both landscape and portrait framing; convert and strip metadata from every replacement before adding it here.
